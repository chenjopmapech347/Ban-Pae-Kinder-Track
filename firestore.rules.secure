rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ปิดการเข้าถึงทั้งหมดจาก client โดยตรง
    // แอปใช้ custom PIN auth — ข้อมูลทั้งหมดผ่าน localStorage + Cloud sync
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
